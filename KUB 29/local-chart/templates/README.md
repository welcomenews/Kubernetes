В этой строке
```
labels:
    {{- include "local-chart.labels" . | nindent 4 }}

{{-  
означает удалить пробелы. Если дифис слева, то съедает пробелы слева, а дифис справа, съедает пробелы справа.

nindent 4 
означает начало с новой строки и отступ 4 пробела.

{{- toYaml .    }}
Мы убеждаемся что значение в текущем контексте, это корректный yaml.

```
